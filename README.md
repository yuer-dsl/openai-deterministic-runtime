# openai-deterministic-runtime
A consolidated repo for deterministic, reproducible AI execution POCs targeting OpenAI, AWS, Google and other LLM platforms — replacing non-deterministic planners with static plans, compiled chains, and predictable runtimes.
